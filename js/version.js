/* ===========================================================
   version.js — single source of truth for the app version.
   Bump this on every release (semantic: MAJOR.MINOR.PATCH)
   and add an entry to CHANGELOG.md.
   =========================================================== */
window.RF = window.RF || {};
RF.VERSION = "1.0.0";
RF.RELEASED = "2026-07-01";
